location  /apollo/docs {
    # Policy configuration here (authentication, rate limiting, logging...)
    # access_log /var/log/nginx/warehouse_api.log main;

    set $upstream_apollo apollo;
    proxy_pass http://$upstream_apollo:4001;
    proxy_set_header    X-Forwarded-For $remote_addr;
}

location /api/movie {
    # Policy configuration here (authentication, rate limiting, logging...)
    # access_log /var/log/nginx/warehouse_api.log main;

    set $upstream_apollo apollo;
    proxy_pass http://$upstream_apollo:4001;
    proxy_set_header    X-Forwarded-For $remote_addr;
}

location /api/series {
    # Policy configuration here (authentication, rate limiting, logging...)
    # access_log /var/log/nginx/warehouse_api.log main;

    set $upstream_apollo apollo;
    proxy_pass http://$upstream_apollo:4001;
    proxy_set_header    X-Forwarded-For $remote_addr;
}

location /api/episode {
    # Policy configuration here (authentication, rate limiting, logging...)
    # access_log /var/log/nginx/warehouse_api.log main;

    set $upstream_apollo apollo;
    proxy_pass http://$upstream_apollo:4001;
    proxy_set_header    X-Forwarded-For $remote_addr;
}

location /api/image {
    # Policy configuration here (authentication, rate limiting, logging...)
    # access_log /var/log/nginx/warehouse_api.log main;

    set $upstream_apollo apollo;
    proxy_pass http://$upstream_apollo:4001;
    proxy_set_header    X-Forwarded-For $remote_addr;
}

# location /api/image {
#     # Policy configuration here (authentication, rate limiting, logging...)
#     # access_log /var/log/nginx/warehouse_api.log main;
#
#     rewrite /api/image(.*) /api/image$1 break;
#     set $upstream_apollo apollo;
#     proxy_pass http://$upstream_apollo:4001;
#     proxy_set_header    X-Forwarded-For $remote_addr;
# }

location /api/video {
    # Policy configuration here (authentication, rate limiting, logging...)
    # access_log /var/log/nginx/warehouse_api.log main;

    set $upstream_apollo apollo;
    proxy_pass http://$upstream_apollo:4001;
    proxy_set_header    X-Forwarded-For $remote_addr;
}

location /api/imdb {
    # Policy configuration here (authentication, rate limiting, logging...)
    # access_log /var/log/nginx/warehouse_api.log main;

    set $upstream_apollo apollo;
    proxy_pass http://$upstream_apollo:4001;
    proxy_set_header    X-Forwarded-For $remote_addr;
}

location /api/actor {
    # Policy configuration here (authentication, rate limiting, logging...)
    # access_log /var/log/nginx/warehouse_api.log main;

    set $upstream_apollo apollo;
    proxy_pass http://$upstream_apollo:4001;
    proxy_set_header    X-Forwarded-For $remote_addr;
}

location /api/director {
    # Policy configuration here (authentication, rate limiting, logging...)
    # access_log /var/log/nginx/warehouse_api.log main;

    set $upstream_apollo apollo;
    proxy_pass http://$upstream_apollo:4001;
    proxy_set_header    X-Forwarded-For $remote_addr;
}

location /api/genre {
    # Policy configuration here (authentication, rate limiting, logging...)
    # access_log /var/log/nginx/warehouse_api.log main;

    set $upstream_apollo apollo;
    proxy_pass http://$upstream_apollo:4001;
    proxy_set_header    X-Forwarded-For $remote_addr;
}

location /api/language {
    # Policy configuration here (authentication, rate limiting, logging...)
    # access_log /var/log/nginx/warehouse_api.log main;

    set $upstream_apollo apollo;
    proxy_pass http://$upstream_apollo:4001;
    proxy_set_header    X-Forwarded-For $remote_addr;
}

location /api/server {
    # Policy configuration here (authentication, rate limiting, logging...)
    # access_log /var/log/nginx/warehouse_api.log main;

    set $upstream_apollo apollo;
    proxy_pass http://$upstream_apollo:4001;
    proxy_set_header    X-Forwarded-For $remote_addr;
}

location /api/studio {
    # Policy configuration here (authentication, rate limiting, logging...)
    # access_log /var/log/nginx/warehouse_api.log main;

    set $upstream_apollo apollo;
    proxy_pass http://$upstream_apollo:4001;
    proxy_set_header    X-Forwarded-For $remote_addr;
}

location /api/writer {
    # Policy configuration here (authentication, rate limiting, logging...)
    # access_log /var/log/nginx/warehouse_api.log main;

    set $upstream_apollo apollo;
    proxy_pass http://$upstream_apollo:4001;
    proxy_set_header    X-Forwarded-For $remote_addr;
}
